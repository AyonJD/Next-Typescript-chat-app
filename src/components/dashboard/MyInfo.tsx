import { Typography } from '@mui/material';
import Card from '../shared/CardShape';

const MyInfo = () => {
    return (
        <Card title="Users chat" >
            <Typography variant="h4">My Info here</Typography>
        </Card>
    );
};

export default MyInfo;
